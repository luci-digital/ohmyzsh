# orchestration/ray

The `ray` service is defined in `../podman/podman-compose.yml`. This dir holds
any ray-specific overrides, configs, or notes. See the compose file for the
canonical service definition (image, ports, healthcheck, IPv6 binding).
