# orchestration/opendeepwiki

The `opendeepwiki` service is defined in `../podman/podman-compose.yml`. This dir holds
any opendeepwiki-specific overrides, configs, or notes. See the compose file for the
canonical service definition (image, ports, healthcheck, IPv6 binding).
