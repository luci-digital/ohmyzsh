# shell — LuciVerse zsh environment

LuciDigital-specific shell configuration (zshrc, custom plugins/themes).
The full upstream **Oh My Zsh** tree is relocated verbatim to
`../legacy/original-layout/` (nothing deleted). Pull OMZ via the flake or
source it from legacy as needed.

- `ohmyzsh/`  — LuciVerse OMZ overlay / bootstrap
- `zshrc/`    — sovereign `.zshrc` fragments
- `plugins/`  — LuciDigital plugins
- `themes/`   — LuciDigital themes
