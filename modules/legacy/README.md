# legacy

Relocated upstream assets, preserved verbatim per the "no permanent delete"
rule. `original-layout/` holds the full Oh My Zsh tree (oh-my-zsh.sh, lib,
plugins, themes, tools, templates, custom, cache, log) and the original OMZ
README. Nothing here is wired into the active platform; it is reference + reuse.
